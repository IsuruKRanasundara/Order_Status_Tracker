# AI assistance notes

Added the requested frontend API, components, and types structure, plus root documentation and ignore rules. Replaced the Vite demo with a simple order tracker starter screen. Preserved the existing backend scaffold and package configuration.

The order model, status values, and endpoints remain undefined. API and type modules are intentional placeholders, and the status filter is disabled until integration is implemented. No database or backend behavior was added.
